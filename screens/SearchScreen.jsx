import React from 'react';
import { StyleSheet, View } from 'react-native';

const SearchScreen = () => <View style={styles.container} />;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#000',
  },
});

export { SearchScreen };
